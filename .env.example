# Скопируйте этот файл в .env и заполните значения
# cp .env.example .env

# Секретный ключ Flask (используется для сессий и CSRF). Сгенерируйте свой:
# python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-me-to-a-random-secret-key

# Строка подключения к БД.
# Если не задавать эту переменную — используется файл bakery.db прямо в
# корне проекта (см. config.py). Раскомментируйте строку ниже, только если
# хотите явно указать путь или Postgres:
# DATABASE_URL=sqlite:///bakery.db
# Для Postgres: postgresql://user:password@host:5432/dbname

# Режим отладки Flask (True только для локальной разработки!)
FLASK_DEBUG=True

# Логин/пароль администратора, создаваемого при первом запуске seed-скрипта
ADMIN_EMAIL=admin@bakery-demo.com
ADMIN_PASSWORD=admin12345
